global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: albion-app
    metrics_path: /api/metrics/prometheus
    static_configs:
      - targets:
          - app:3000
